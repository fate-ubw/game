# 🐍 贪吃蛇游戏 (Snake Game)

一个使用 Vue 3 + Vite 开发的经典贪吃蛇游戏，支持 GitHub Pages 部署。

## 🌐 在线访问

开发服务器: http://localhost:5174/game/

## ✨ 功能特性

- 🎮 经典贪吃蛇游戏玩法
- 🎨 现代化的 UI 设计
- 📱 响应式布局，支持移动端
- ⌨️ 键盘控制（方向键）
- 🖱️ 触摸按钮控制
- 🏆 本地最高分记录
- ⚙️ 三种难度选择（简单/中等/困难）
- ⏸️ 暂停/继续功能

## 🚀 快速开始

### 环境要求

- Node.js >= 20.x
- npm >= 10.x

### 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 🎮 游戏操作

### 键盘控制
- ⬆️ 方向键上：向上移动
- ⬇️ 方向键下：向下移动
- ⬅️ 方向键左：向左移动
- ➡️ 方向键右：向右移动
- 空格键：暂停/继续

### 触摸控制
- 使用屏幕上的方向按钮控制蛇的移动

## 📦 部署到 GitHub Pages

### 方法一：使用 GitHub Actions（推荐）

1. 在 GitHub 仓库中启用 GitHub Pages
   - 进入仓库的 Settings > Pages
   - Source 选择 "GitHub Actions"

2. 推送代码到 main 分支
   ```bash
   git add .
   git commit -m "Initial commit"
   git push origin main
   ```

3. GitHub Actions 会自动构建和部署

### 方法二：手动部署

1. 更新 `package.json` 中的 deploy 脚本，替换为你的仓库地址：
   ```json
   "deploy": "npm run build && cd dist && git init && git add -A && git commit -m 'deploy' && git push -f git@github.com:YOUR_USERNAME/YOUR_REPO.git main:gh-pages"
   ```

2. 运行部署命令：
   ```bash
   npm run deploy
   ```

3. 在 GitHub 仓库设置中启用 GitHub Pages，选择 gh-pages 分支

## 🛠️ 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 下一代前端构建工具
- **Canvas API** - 游戏渲染
- **LocalStorage** - 本地数据存储

## 📁 项目结构

```
snake-game/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions 部署配置
├── public/                     # 静态资源
├── src/
│   ├── components/
│   │   └── SnakeGame.vue      # 贪吃蛇游戏组件
│   ├── App.vue                # 根组件
│   ├── main.js                # 入口文件
│   └── style.css              # 全局样式
├── index.html                 # HTML 模板
├── package.json               # 项目配置
├── vite.config.js             # Vite 配置
└── README.md                  # 项目说明
```

## 🎯 游戏规则

1. 控制蛇吃到红色食物
2. 每吃一个食物得 10 分
3. 蛇身会随着吃到的食物增长
4. 撞到墙壁或自己的身体游戏结束
5. 挑战更高分数！

## 📝 配置说明

### 修改 base 路径

如果你的 GitHub Pages 不是部署在根路径，需要修改 `vite.config.js` 中的 `base` 选项：

```javascript
export default defineConfig({
  base: '/your-repo-name/',  // 改为你的仓库名
  // ...
})
```

## 📄 License

MIT
