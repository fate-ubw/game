# 📁 项目结构说明

## ✅ 优化后的结构（当前）

```
game/                           # Git 仓库根目录 + Vue 项目根目录
├── .git/                       # Git 配置
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions 自动部署
├── .gitignore                  # Git 忽略文件
├── node_modules/               # 依赖包
├── public/                     # 静态资源
│   └── vite.svg
├── src/                        # 源代码
│   ├── assets/                 # 资源文件
│   │   └── vue.svg
│   ├── components/             # Vue 组件
│   │   ├── HelloWorld.vue      # 默认组件（可删除）
│   │   ├── SnakeGame.vue       # 贪吃蛇游戏主组件
│   │   ├── StateDemo.vue       # 状态管理演示组件
│   │   └── Navigation.vue      # 导航组件
│   ├── App.vue                 # 根组件
│   ├── main.js                 # 入口文件
│   └── style.css               # 全局样式
├── index.html                  # HTML 模板
├── package.json                # 项目配置和依赖
├── package-lock.json           # 依赖锁定文件
├── vite.config.js              # Vite 配置
├── README.md                   # 项目说明（英文）
├── DEPLOYMENT.md               # 部署指南
├── PROJECT_SETUP.md            # 项目设置说明
├── 快速开始.md                  # 快速开始指南（中文）
├── 条件渲染教程.md               # Vue 条件渲染教程
└── 理解条件渲染.md               # 针对性的条件渲染解释
```

---

## 🎯 为什么这个结构更好？

### ✅ 优点

1. **结构清晰**
   - Git 仓库和 Vue 项目在同一层级
   - 不需要嵌套的子目录

2. **部署简单**
   - GitHub Actions 可以直接找到项目文件
   - 不需要配置额外的工作目录

3. **路径一致**
   - `vite.config.js` 中的 `base: '/game/'` 对应仓库名
   - 部署后访问: `https://YOUR_USERNAME.github.io/game/`

4. **易于维护**
   - 所有配置文件在根目录
   - 不会混淆项目边界

### ❌ 之前的问题（已解决）

```
game/                    # Git 仓库
└── snake-game/         # Vue 项目（嵌套）
    ├── src/
    └── ...
```

问题：
- ❌ GitHub Actions 需要配置 `working-directory`
- ❌ 路径配置容易出错
- ❌ 结构不够直观

---

## 📊 文件说明

### 核心文件

| 文件 | 作用 |
|------|------|
| `src/components/SnakeGame.vue` | 贪吃蛇游戏的核心逻辑和 UI |
| `src/components/StateDemo.vue` | 交互式状态管理演示 |
| `src/components/Navigation.vue` | 游戏和演示之间的切换导航 |
| `src/App.vue` | 应用根组件 |
| `src/main.js` | 应用入口，挂载 Vue 实例 |
| `vite.config.js` | Vite 构建配置 |
| `package.json` | 项目元数据和依赖 |

### 配置文件

| 文件 | 作用 |
|------|------|
| `.github/workflows/deploy.yml` | GitHub Actions 自动部署配置 |
| `.gitignore` | Git 忽略规则 |
| `vite.config.js` | Vite 配置（base 路径、构建选项） |

### 文档文件

| 文件 | 内容 |
|------|------|
| `README.md` | 项目完整说明（功能、技术栈、部署） |
| `DEPLOYMENT.md` | 详细的部署指南 |
| `PROJECT_SETUP.md` | 项目初始化记录 |
| `快速开始.md` | 中文快速入门 |
| `条件渲染教程.md` | Vue 条件渲染完整教程 |
| `理解条件渲染.md` | 针对"一个 template 多个页面"的解释 |

---

## 🔧 关键配置

### vite.config.js

```javascript
export default defineConfig({
  plugins: [vue()],
  base: '/game/',        // GitHub Pages 路径
  build: {
    outDir: 'dist',      // 构建输出目录
  }
})
```

**说明**：
- `base: '/game/'` 对应 GitHub 仓库名
- 如果仓库名改变，需要同步修改这里

### package.json

```json
{
  "name": "snake-game",
  "scripts": {
    "dev": "vite",              // 开发服务器
    "build": "vite build",      // 构建生产版本
    "preview": "vite preview",  // 预览生产构建
    "deploy": "..."             // 手动部署脚本
  }
}
```

---

## 🚀 启动项目

### 当前状态
✅ 开发服务器已在运行: **http://localhost:5174/game/**

### 如果需要重新启动

```bash
# 1. 进入项目目录
cd /Users/jim/0-Phd/Back-to-future/Web2Tool/webNavigator-homepage/game

# 2. 启动开发服务器
npm run dev

# 3. 打开浏览器访问
# http://localhost:5174/game/
```

---

## 📦 依赖说明

### 生产依赖
- `vue: ^3.5.25` - Vue 3 框架

### 开发依赖
- `vite: ^7.3.1` - 构建工具
- `@vitejs/plugin-vue: ^6.0.2` - Vue 插件

所有依赖已安装在 `node_modules/` 目录。

---

## 🌳 Git 仓库信息

- **仓库位置**: `/Users/jim/0-Phd/Back-to-future/Web2Tool/webNavigator-homepage/game`
- **当前分支**: main
- **远程仓库**: 需要配置（用于 GitHub Pages 部署）

---

## 📝 下一步

1. **本地开发**: 访问 http://localhost:5174/game/ 开始玩
2. **学习原理**: 点击"🎓 学习状态管理"按钮
3. **准备部署**: 配置 GitHub 远程仓库
4. **推送代码**: `git push origin main`

---

## 💡 提示

- 修改 `.vue` 文件会自动热重载
- 不需要重启服务器
- 游戏状态会在热重载时保持
